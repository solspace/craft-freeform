{
  "name": "@ff/scripts",
  "version": "1.0.0",
  "scripts": {
    "dev": "pnpm run clean && pnpm run watch",
    "build": "pnpm run clean && node build.mjs --production",
    "watch": "node build.mjs --watch",
    "clean": "rm -rf ../plugin/src/Resources/js/scripts",
    "lint": "biome check",
    "lint:fix": "biome check --write",
    "compile": "node build.mjs --production",
    "test": ""
  },
  "author": "Solspace Inc.",
  "license": "MIT",
  "dependencies": {
    "@paypal/paypal-js": "^9.4.0",
    "@stripe/stripe-js": "^8.11.0",
    "expression-language": "^2.5.4"
  },
  "devDependencies": {
    "@types/grecaptcha": "^3.0.9",
    "@types/jquery": "^4.0.0",
    "esbuild": "^0.27.4",
    "glob": "^13.0.6",
    "typescript": "^5.9.3"
  }
}
