<div class="fullwidth">
  <select class="fullwidth" multiple>
    <% generatedOptions.forEach(function(option) { %>
        <option value="<%= option.value %>"
                <% if (option.checked) { %>selected<% } %>
        >
            <%= option.label %>
        </option>
    <% }); %>
  </select>
</div>
