{% extends "freeform/_layouts/settings" %}
{% import "_includes/forms" as forms %}

{% set title = "Surveys & Polls"|t("freeform") %}

{% block content %}

    <input type="hidden" name="action" value="freeform/surveys-settings/save">
    {{ redirectInput('freeform/settings/surveys') }}
    {{ csrfInput() }}

    <h2 class="first">{{ "Surveys & Polls"|t('freeform-surveys') }}</h2>

    {{ forms.lightswitchField({
        label: "Highlight highest ranking option?"|t('freeform-surveys'),
        instructions: "When enabled, Freeform will highlight the highest ranking option chosen for each question with an alternate color."|t('freeform-surveys'),
        name: "highlightHighest",
        on: settings.highlightHighest,
        errors: settings.errors("highlightHighest"),
    }) }}

    <hr>

    {% set chartTypes %}
        <table id="chart-types">
            {% for fieldClass, chartType in settings.chartTypes %}
                {% set type = fieldTypes[fieldClass] %}
                <tr>
                    <th>
                        <div>
                            <div class="icon">{{ type.icon|raw }}</div>
                            <div class="name">{{ type.name }}</div>
                        </div>
                    </th>
                    <td>
                        {{ forms.select({
                            name: "chartTypes["~fieldClass~"]",
                            value: settings.chartTypes[fieldClass],
                            options: options,
                        }) }}
                    </td>
                </tr>
            {% endfor %}
        </table>
    {% endset %}



    {{ forms.field({
        label: "Survey & Poll Chart Types"|t('freeform-surveys'),
        instructions: "Select the default chart types to use for each field type."|t('freeform-surveys'),
    }, chartTypes) }}

{% endblock %}

{% css %}

    table#chart-types {
        td, th {
            padding: 0 0 8px;
            margin: 0;
        }

        th > div {
            display: flex;
            gap: 5px;

            padding-right: 40px;
        }

        & .icon svg {
            width: 20px;
            height: 20px;
        }
    }

{% endcss %}
