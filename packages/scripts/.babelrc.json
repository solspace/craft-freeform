{
  "presets": [["@babel/preset-env", { "targets": "last 2 versions, ie >= 10" }]],
  "plugins": [
    [
      "@babel/plugin-proposal-class-properties",
      {
        "loose": true
      }
    ]
  ]
}
