{# Pull in CSS and field rendering #}
<style>
{% include "freeform/_defaultFormTemplates/multipage-all-fields/_main.css" %}
</style>
{% import "freeform/_defaultFormTemplates/multipage-all-fields/_row.twig" as rowMacro %}

{# Render the opening form tag #}
{{ form.renderTag({
    attributes: {
        row: { class: "freeform-row" },
        success: { class: "freeform-form-success" },
        errors: { class: "freeform-form-has-errors" },
    }
}) }}

{# Pull in JS overrides #}
<script>
{% include "freeform/_defaultFormTemplates/multipage-all-fields/_main.js" %}
</script>

{# Success and error message handling for non-AJAX forms #}
{% if not form.settings.ajax %}
    {% if form.submittedSuccessfully %}
        <div class="freeform-alert freeform-alert-success">
            {{ form.settings.successMessage | t('freeform') }}
        </div>
    {% endif %}
    {% if form.hasErrors %}
        <div{{ form.hasErrors }}>
            {{ form.settings.errorMessage | t('freeform') }}

            {% if form.errors|length %}
                <ul>
                    {% for error in form.errors %}
                        <li>{{ error }}</li>
                    {% endfor %}
                </ul>
            {% endif %}
        </div>
    {% endif %}
{% endif %}

{# Display form field rows and columns #}
{{ rowMacro.render(form.rows, form) }}

{# Render the closing form tag #}
{{ form.renderClosingTag }}