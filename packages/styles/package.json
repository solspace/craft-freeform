{
  "name": "@ff/styles",
  "version": "1.0.0",
  "scripts": {
    "dev": "pnpm run clean && sass --no-source-map --watch src/:../plugin/src/Resources/css",
    "build": "pnpm run clean && sass --no-source-map --style=compressed src/:../plugin/src/Resources/css",
    "clean": "rm -rf ../plugin/src/Resources/css",
    "lint": "biome check",
    "lint:fix": "biome check --write",
    "test": ""
  },
  "author": "Solspace Inc.",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "sass": "^1.102.0"
  }
}
