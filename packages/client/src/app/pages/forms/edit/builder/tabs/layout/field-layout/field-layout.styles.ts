import styled from 'styled-components';

export const FieldLayoutWrapper = styled.div`
  display: flex;
  flex-direction: column;
  align-items: stretch;

  overflow: hidden;

  flex: 1;
  padding: var(--padding);
`;
