{
  "name": "@ff/styles",
  "version": "1.0.0",
  "scripts": {
    "dev": "npm run clean && npm run build && sass --no-source-map --watch src/:../plugin/src/Resources/css",
    "build": "npm run clean && sass --no-source-map src/:../plugin/src/Resources/css",
    "clean": "rm -rf ../plugin/src/Resources/css",
    "test": ""
  },
  "author": "Solspace Inc.",
  "license": "MIT",
  "devDependencies": {
    "sass": "^1.34.1",
    "y18n": ">=4.0.1"
  },
  "dependencies": {
    "craftcms-sass": "^3.5.1",
    "react-tippy": "^1.4.0"
  }
}
