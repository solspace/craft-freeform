<div class="select fullwidth">
    <select>
        <% generatedOptions.forEach(function(option) { %>
            <option value="<%= option.value %>"
                    <% if (option.value === defaultValue) { %>selected<% } %>
            >
                <%= option.label %>
            </option>
        <% }); %>
    </select>
</div>
