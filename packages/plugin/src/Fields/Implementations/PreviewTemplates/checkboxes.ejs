<% generatedOptions.forEach(function(option) { %>
    <div>
        <input type="checkbox"
               class="checkbox"
               <% if (defaultValue.includes(option.value)) { %>checked="checked"<% } %>
        />
        <label for=""><%= option.label %></label>
    </div>
<% }) %>
