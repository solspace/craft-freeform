{% set selectedValue = submission[field.handle] is not null ? submission[field.handle].value : '' %}
{% do field.setValue(selectedValue) %}

<div class="field">
    <div class="heading">
        <label>{{ field.label }}</label>
    </div>
    <div>
        {{ field.renderInput }}
    </div>
</div>
