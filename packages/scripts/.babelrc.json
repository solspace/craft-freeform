{
  "presets": [["@babel/preset-env", { "targets": "last 2 versions, ie >= 10" }], ["@babel/preset-typescript"]],
  "plugins": [
    [
      "@babel/plugin-proposal-class-properties",
      {
        "loose": true
      }
    ]
  ]
}
