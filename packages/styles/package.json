{
  "name": "@ff/styles",
  "version": "1.0.0",
  "scripts": {
    "dev": "pnpm run clean && pnpm run build && sass --no-source-map --watch src/:../plugin/src/Resources/css",
    "build": "pnpm run clean && sass --no-source-map --style=compressed src/:../plugin/src/Resources/css",
    "clean": "rm -rf ../plugin/src/Resources/css",
    "test": ""
  },
  "author": "Solspace Inc.",
  "license": "MIT",
  "devDependencies": {
    "sass": "1.102.0"
  },
  "dependencies": {
    "craftcms-sass": "^3.5.1",
    "react-tippy": "^1.4.0"
  }
}
