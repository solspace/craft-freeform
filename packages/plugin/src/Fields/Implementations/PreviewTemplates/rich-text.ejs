<%
    const notConfiguredText =
        (typeof Craft !== 'undefined' && Craft.t)
            ? Craft.t('freeform', 'This Rich Text field has not been configured yet.')
            : 'This Rich Text field has not been configured yet.';
%>

<% if (!content) { %>
    <%= notConfiguredText %>
<% } %>

<%= content %>