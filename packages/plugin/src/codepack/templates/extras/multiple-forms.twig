{% extends "demo/_layout" %}

{% set pageTitle = "Extras" %}
{% set page = "extras" %}

{# Replace 'freeformMultipleForms' with your form handle. #}
{% set form = craft.freeform.form("freeformMultipleForms") %}

{% block content %}

<div class="container">

    <div class="my-5">
        <h2>Multiple Forms in the Same Page</h2>
        <p>This example shows how you might have the same form (or 2 similar ones) more than once in the same template. Some complex field types and features might otherwise behave incorrectly unless some adjustments are made to the templates.</p>
    </div>

    {% if form %}

        <h3 class="mb-3">{{ form.name }} <small class="text-muted">(1st instance)</small></h3>

        {# Apply 'id' and 'fieldIdPrefix' parameters with unique values #}
        {{ form.render({
            id: 'first-instance',
            fieldIdPrefix: 'first-form-'
        }) }}

        <h3 class="mb-3">{{ form.name }} <small class="text-muted">(2nd instance)</small></h3>

        {# Apply 'id' and 'fieldIdPrefix' parameters with unique values #}
        {{ form.render({
            id: 'second-instance',
            fieldIdPrefix: 'second-form-'
        }) }}

    {% else %}
    {# Show instructions to developer if they wish to see this page work correctly #}

        <div class="alert alert-warning">
            <h4 class="alert-heading mb-3">You can't see this page correctly until...</h4>
            <p>
                In order to view this page live, you'll need to make some adjustments to this template or your test form:
                <ol class="mb-0">
                    <li>Rename your test form handle to <code>freeformMultipleForms</code> or adjust the form handle name inside this template to match your test form.</li>
                </ol>
            </p>
        </div>
    
    {% endif %}

</div>

{% endblock %}
