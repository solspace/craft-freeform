{% do field.setValue(submission[field.handle].value) %}

<div class="field">
    <div class="heading">
        <label>{{ field.label }}</label>
    </div>
    <div>
        {{ field.renderInput }}
    </div>
</div>
