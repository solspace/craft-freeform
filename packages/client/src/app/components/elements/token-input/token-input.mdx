import { Canvas, Meta } from '@storybook/blocks';
import TokenInputStories, { Default } from './token-input.stories';

<Meta of={TokenInputStories} />

# Token Input

This input lets you tokenize your choices with providing existing options from a dropdown.

<Canvas of={Default} />
