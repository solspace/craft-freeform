<% generatedOptions.forEach(function(option) { %>

    <% if (oneLine) { %><span><% } else { %><div><% } %>

        <input type="radio"
               class="radio"
               <% if (option.value === defaultValue) { %>checked="checked"<% } %>
        />
        <label for=""><%= option.label %></label>

    <% if (oneLine) { %><span><% } else { %></div><% } %>

<% }) %>
