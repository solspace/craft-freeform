{% do field.setParameters({
    attributes: {
        input: { "=class": "form-check-input bg-dark-subtle" },
        label: { class: "form-check-label text-white" },
    }
}) %}

<div{{ field.attributes.container }}>
    <div class="form-check">
        {{ field.renderInput }}
        {{ field.renderLabel }}
        {{ field.renderInstructions }}
        {{ field.renderErrors }}
    </div>
</div>